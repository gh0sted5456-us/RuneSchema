# LoadTight1 validation

consume-queue.cpp exercises the actual helper against the previous erase loop, including every exception position for all removal masks through nine entries. asset-batch.cpp and early-table-replay.cpp contain production-extracted bodies with engine stubs. unreal-readiness.cpp exercises the unchanged readiness gate. The local outputs/loading-efficiency/make-tests.py regenerates extracted bodies from this candidate using PakLayout1 harnesses. These checks do not emulate native rendering, GC, game load timing or network behavior.
